# ================================================
# Server Environment Variables (Express + GraphQL)
# ================================================
# Copy this file to:
#   server/.env
#
# These values are loaded by the server at runtime.
# ================================================


# ---------------------------
# Server Port
# ---------------------------
# The Express server will listen on this port.
# Default: 4000
PORT=4000


# ---------------------------
# MongoDB Connection String
# ---------------------------
# Local development (default):
#   mongodb://localhost:27017/ks_baseplate
#
# MongoDB Atlas example:
#   mongodb+srv://<user>:<pass>@<cluster>/ks_baseplate
#
# NOTE:
# - The code expects `MONGO_URI`
# - This branch requires a valid connection string
MONGO_URI=mongodb://localhost:27017/ks_baseplate


# ============================================
# Auth0 Configuration (REQUIRED for this branch)
# ============================================
# These values configure JWT validation for the
# protected GraphQL API. The server enforces auth
# on all /graphql requests.
#
# AUTH0_DOMAIN:
#   - From Auth0 Dashboard → Applications → {App} → Domain
#   - DO NOT include "https://"
#
# AUTH0_AUDIENCE:
#   - From Auth0 Dashboard → APIs → {API} → API Audience
#   - Must match the client VITE_AUTH0_AUDIENCE value
# ============================================

AUTH0_DOMAIN=
AUTH0_AUDIENCE=
